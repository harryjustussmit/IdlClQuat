/* vim: set syntax=magma :*/

    p:=3;
    s:=2;
    B<i, j, k> := QuaternionAlgebra(RationalField(), -1, -p);
    O1 := MaximalOrder(B);
    O := QuaternionOrder([s*i,s*j,s*k]);

    I:=rideal<O|[ 1, i, 2*j, k ]>;
    J:=rideal<O|[ 5, i + 2*j + 23*k, 1 + 2*j + 2*k, 5*k ]>;
    test,_,x:=IsRightIsomorphic(I,J);
    test;
    xI:=rideal<O|[x*z : z in ZBasis(I)]>;
    xI eq J;
    [ z in J | z in ZBasis(xI)];
    [ z in xI | z in ZBasis(J)];

    AttachSpec("~/IdlClQuat/spec");
    Attach("~/IdlClQuat/dev/BrandtMat_naive.m");

    p:=3;
    s:=2;
    B<i, j, k> := QuaternionAlgebra(RationalField(), -1, -p);
    O1 := MaximalOrder(B);
    O := QuaternionOrder([s*i,s*j,s*k]);

    I:=rideal<O|[ 1, i, 2*j, k ]>;
    J:=rideal<O|[ 5, i + 2*j + 23*k, 1 + 2*j + 2*k, 5*k ]>;
    test,_,x:=IsRightIsomorphic(I,J);
    test;
    xI:=rideal<O|[x*z : z in ZBasis(I)]>;
    xI eq J;
    [ z in J | z in ZBasis(xI)];
    [ z in xI | z in ZBasis(J)];
